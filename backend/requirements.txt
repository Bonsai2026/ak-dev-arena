# AK Dev Arena — backend dependencies
# Pinned to the exact versions the full suite (backend tests + CI matrix)
# was verified against, so CI can't drift silently. Bump deliberately.
fastapi==0.141.1
uvicorn[standard]==0.52.4
litellm==1.100.1
pyyaml==6.0.3
pydantic==2.13.5
httpx==0.28.1
pytest==9.1.1
python-multipart==0.0.32  # file uploads (voice mode)
