# AK Dev Arena — pytest config
# `pythonpath = .` puts the repo root on sys.path so `from backend.app import ...`
# works no matter how pytest is launched (bare `pytest` or `python -m pytest`),
# including in CI (Linux + Windows jobs).
[pytest]
pythonpath = .
testpaths = backend/tests
